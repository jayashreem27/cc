FROM python:3.9

WORKDIR /app

# Copy the requirements file from the correct location.
COPY ./requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

# Copy the api package so that the package structure is preserved.
COPY ./api /app/api

# Run the consumer as a module to allow proper absolute imports.
CMD ["python", "-m", "api.consumer"]

